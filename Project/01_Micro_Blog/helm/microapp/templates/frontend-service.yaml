apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-frontend-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: frontend
    release: {{ .Release.Name }}
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: frontend
    release: {{ .Release.Name }}
