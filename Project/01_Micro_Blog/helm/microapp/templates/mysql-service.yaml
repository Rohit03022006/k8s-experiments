apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mysql
  namespace: {{ .Values.namespace }}
  labels:
    app: mysql
    release: {{ .Release.Name }}
spec:
  clusterIP: None
  ports:
  - port: 3306
  selector:
    app: mysql
    release: {{ .Release.Name }}
