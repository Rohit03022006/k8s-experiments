apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-backend-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: backend
    release: {{ .Release.Name }}
spec:
  ports:
  - port: 3000
    targetPort: 3000
  selector:
    app: backend
    release: {{ .Release.Name }}
